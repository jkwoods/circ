

def karp_rabin(public u8[3] p, public u8[10] x, public u8[3] r) -> u8:

  u8[3] r2 = [0, 0, 0]
  for u8 i in 0..9 do
    r2[i] = if x[i] == 0x2a then 0 else r[i] fi
  endfor  

  u8 fpr = 0 
  for u8 i in 0..2 do
     u8 temp = p[i]*r[i]
     fpr = fpr + temp
  endfor


  return 0


def main() -> u8:

  u8[3] p = [0x01, 0x2a, 0x03]
  u8[10] x = [0x01, 0x20, 0x03, 0x44, 0x44, 0x44, 0x44, 0x44, 0x44, 0x44]
  u8[3] r = [0x08, 0x50, 0x13]

  u8 out = karp_rabin(p,x,r)
  return out
